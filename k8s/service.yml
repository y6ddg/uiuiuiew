apiVersion: v1
kind: Service
metadata:
  name: uiuiuiew
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uiuiuiew"
    port: 8080
  selector:
    app: uiuiuiew
